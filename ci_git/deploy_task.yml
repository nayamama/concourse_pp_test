---
platform: linux

image_resource:
  type: docker-image
  source: {repository: ubuntu}

inputs:
  - name: test-repo
  - name: upload_folder
    #path: upload_folder/
run:
  #path: test-repo/ci_git/deploy_task.sh
  path: ls
  args: ['alR']

